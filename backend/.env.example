DATABASE_URL=postgres://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=change-me-to-a-long-random-secret
PORT=8080

# Email — Option 1: Resend HTTP API (recommended for Render / cloud platforms)
RESEND_API_KEY=re_xxxxxxxxxxxx
SMTP_FROM=RetailOS <onboarding@resend.dev>

# Email — Option 2: Gmail SMTP (works locally, blocked on most PaaS)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USERNAME=retailos.noreply@gmail.com
# SMTP_PASSWORD=your-gmail-app-password
# SMTP_FROM=RetailOS <retailos.noreply@gmail.com>
